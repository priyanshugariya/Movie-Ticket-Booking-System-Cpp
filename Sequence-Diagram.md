# E. Sequence Diagram — Customer books 1 seat and pays by UPI

The diagram shows the required lifelines and the flow from selecting a show through ticket printing. It includes synchronous calls, returns, activation-oriented service calls, and `<<create>>` messages for `Booking` and `UpiPayment`.
